apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bottleneck-bots-production
  namespace: argocd
  labels:
    app.kubernetes.io/name: bottleneck-bots
    app.kubernetes.io/part-of: bottleneck-bots
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/ghl-agency-ai/ghl-agency-ai.git
    targetRevision: main
    path: helm/bottleneck-bots
    helm:
      valueFiles:
        - ../../gitops/overlays/production/values.yaml
      parameters:
        - name: image.tag
          value: latest  # Will be overridden by CI/CD

  destination:
    server: https://kubernetes.default.svc
    namespace: bottleneck-bots-production

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: autoscaling
      kind: HorizontalPodAutoscaler
      jsonPointers:
        - /spec/minReplicas
        - /spec/maxReplicas
